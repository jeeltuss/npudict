[whisper]
# lemonade-server URL
endpoint = http://localhost:8000
# Whisper model name
model = whisper-v3-turbo-FLM
# Language code (ISO-639-1) or "auto" for auto-detect
language = it

[hotkey]
key = ctrl_r

[behavior]
auto_type = true
notifications = false
